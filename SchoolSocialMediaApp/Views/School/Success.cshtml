@model SchoolViewModel
@{
    ViewBag.Title = "Success";
}

<h1>@ViewBag.Title</h1>

<h2>You have successfully registered your school :)</h2>

@*<div class="card text-black mb-3" style="max-width: 25rem;">
    <div class="card-header">@Model.Name</div>
    <img class="card-img-top img img-fluid" src="~/images/defaultSchool.png" alt="Card image cap">
    <div class="card-body">
        <h5 class="card-title">@Model.Location</h5>
        <p class="card-text">@Model.Description</p>
    </div>
</div>*@

<div class="card text-black mb-3" style="max-width: 25rem;">
    <div class="card-header">
        <h3 class="text-dark">School Details</h3>
    </div>
    <div class="card-body text-center">
        <h5 class="card-title">@Model.Name</h5>
        <img class="card-img-top img" src="@Model.ImageUrl" alt="School image">
        <p class="card-text">Location: @Model.Location</p>
        <p class="card-text">Description: @Model.Description</p>
        <a href="@Url.Action("Details","School")" class="btn btn-primary">Details</a>
    </div>
</div>

<a class="btn btn-info btn-group-lg" href="@Url.Action("Index", "Home")">Back to Home</a>