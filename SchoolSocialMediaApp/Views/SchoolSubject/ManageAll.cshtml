@model List<SchoolSubjectViewModel>;

@{
    ViewBag.Title = "Manage all school subjects";
}

<h1>@ViewBag.Title</h1>
@if (ViewBag.Message != null)
{
    <h2 class="badge @ViewBag.ClassOfMessage">@ViewBag.Message</h2>
}

@if (Model.Any())
{
    <div class="row container-fluid">
        <table class="table table-striped table-hover table-responsive-md">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Teacher</th>
                    <th>Number Of Classes</th>
                    <th>Created Date & Time</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var schoolSubject in Model)
                {
                    <tr>
                        <td>@schoolSubject.Name</td>
                        <td>@schoolSubject.Teacher!.FirstName + @schoolSubject.Teacher.LastName</td>
                        <td>@schoolSubject.Classes.Count</td>
                        <td>@schoolSubject.CreatedOn</td>
                        <td>
                            <form asp-controller="SchoolClass" asp-action="Manage" method="get">
                                <input type="hidden" name="classId" value="@schoolSubject.Id" />
                                <button type="submit" class="btn btn-primary">Manage</button>
                            </form>
                        </td>
                        <td>
                            <form asp-controller="SchoolClass" asp-action="Delete" method="post">
                                <input type="hidden" name="classId" value="@schoolSubject.Id" />
                                <button type="submit" class="btn btn-secondary">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="row">
        <div class="col-md-4">
        </div>
        <div class="col-md-4">
            @* <a class="btn btn-primary container-fluid" asp-controller="Invitation" asp-action="DeleteAll" asp-route-schoolId="@Model.FirstOrDefault()?.SchoolId">Delete All</a> *@
            <a class="btn btn-primary container-fluid" asp-controller="SchoolSubject" asp-action="Create" asp-route-schoolId="@Model.FirstOrDefault()?.SchoolId">Create New Subject</a>
        </div>
    </div>
}
else
{
    <h2>No school subjects found.</h2>
    <a class="btn btn-primary container-fluid" asp-controller="SchoolSubject" asp-action="Create" asp-route-schoolId="@Model.FirstOrDefault()?.SchoolId">Create new subject</a>
}
